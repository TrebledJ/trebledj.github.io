<!DOCTYPE html>
<html lang="{{ site.language | default("en") }}">
    {% include "head.html" %}
    <body>
        {% include "header.html" %}
        {% if not site.banner.sticky %}
            {% include "banner.html" %}
        {% endif %}
        {{ content | safe }}
        {% include "footer.html" %}

        {# Export post frontmatter options to JS scripts. #}
        <script>
            var tocOptions = '{{ tocOptions | safe }}';
            var site = {
                typewrite: {{ site.typewrite | dump | safe }},
                search: {{ site.search | dump | safe }},
                lightbox: {{ site.lightbox | dump | safe }},
                banner: {{ site.banner | dump | safe }},
            };
            switch (site.banner.scope) {
                case 'local':
                    site.banner.scope = localStorage;
                    break;
                case 'session':
                    site.banner.scope = sessionStorage;
                    break;
                default:
                    site.banner.scope = undefined;
                    break;
            }
        </script>

        <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <script defer src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>

        <script defer src="https://cdn.jsdelivr.net/npm/lunr@2.3.9/lunr.min.js"></script>

        {% js %} {% include "assets/js/head.js" %} {% endjs %}
        {% js %} {% include "assets/js/post.js" %} {% endjs %}
        {% js %} {% include "assets/js/search.js" %} {% endjs %}
        {% js %} {% include "assets/js/404.js" %} {% endjs %}
        <script defer src="{% getBundleFileUrl "js" %}"></script>

        {# <script defer src="/js/head.js"></script>
        <script defer src="/js/search.js"></script>

        {% if assets %}
            {% if assets.js %}
                {% for asset in assets.js %}
                    <script defer src="{{asset}}"></script>
                {% endfor %}
            {% endif %}
        {% endif %} #}

        {% if use_math %}
            {# {% include "utilities/mathjax.html" %} #}
            {% include "utilities/katex.html" %}
        {% endif %}
        {% if site.analytics %}
            {% include "utilities/analytics.html" %}
        {% endif %}
    </body>
</html>